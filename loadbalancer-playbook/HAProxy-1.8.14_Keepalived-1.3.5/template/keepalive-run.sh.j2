docker run -itd --restart=always \
    --name=Keepalived --net=host \
    --cap-add=NET_ADMIN \
    -v /var/lib/wise2c/loadbalancer/keepalive.cfg:/etc/keepalived/keepalived.conf \
    -v /var/lib/wise2c/loadbalancer/keepalived.sh:/usr/bin/keepalived.sh \
    --entrypoint bash \
    wise2c/k8s-keepalived:1.3.5 /usr/bin/keepalived.sh
